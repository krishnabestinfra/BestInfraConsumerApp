/**
 * Design tokens: spacing, radius, shadows.
 * Use in StyleSheet so themes and future design updates are one-place changes.
 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
};

export const radius = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999,
};

export const listDefaults = {
  initialNumToRender: 12,
  maxToRenderPerBatch: 8,
  windowSize: 6,
};
