/**
 * Base font sizes used across the app (default / original design).
 * When user selects "Default" in Settings, these values are used as-is.
 * When user selects 14, 15, or 16, text is scaled proportionally from these bases.
 */
export const fontSizes = {
  tiny: 8,
  caption: 10,
  small: 12,
  body: 14,
  bodyLarge: 16,
  subhead: 18,
  title: 20,
  headline: 24,
  display: 28,
};
